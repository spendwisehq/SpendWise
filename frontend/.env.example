# frontend/.env.example — copy to .env and fill in your values

# Backend API base URL (Vite proxies /api to this in dev — only needed for production builds)
VITE_API_URL=http://localhost:5000/api

VITE_APP_NAME=SpendWise
VITE_APP_VERSION=1.0.0
